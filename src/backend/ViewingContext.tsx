/**
 * @deprecated (#1) Moved to `@/contexts/ViewingContext`.
 * This file is kept temporarily as a re-export so any third-party
 * import that pinned to the old path keeps working. Update your import.
 */
export {
  ViewingProvider,
  useViewing,
} from "@/contexts/ViewingContext";
