/**
 * Shared constants that are too small to deserve their own module but
 * should not be duplicated string-literally across the codebase.
 */

/**
 * The single source of truth for the "Last updated:" stamp shown on the
 * /privacy and /terms pages. Update here when the policy actually
 * changes; both pages will pick it up automatically.
 */
export const LEGAL_LAST_UPDATED = "March 2026";
